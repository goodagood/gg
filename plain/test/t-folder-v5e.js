// Generated by CoffeeScript 1.8.0
(function() {
  var Promise, assert, async, data, fm, folder_path, gg_folder_name, new_folder_name, p, path, stop, u, user_name;

  assert = require("assert");

  u = require("underscore");

  async = require("async");

  path = require("path");

  Promise = require("bluebird");

  data = require("./config.js");

  fm = require("../aws/folder-v5.js");

  folder_path = 'abc';

  user_name = 'abc';

  gg_folder_name = 'goodagood';

  new_folder_name = 'test';

  p = console.log;

  stop = function() {
    return setTimeout(process.exit, 500);
  };

  describe("t-folder-v5e, 1", function() {
    return it("build file list", function(done) {
      return fm.retrieve_promisified_folder(user_name).then(function(folder) {
        return folder;
      }).then(function(f) {
        var Meta;
        return Meta = f.get_meta();
      }).then(function(m) {
        var ul;
        ul = m.renders.ul;
        assert(u.isString(ul));
        return done();
      });
    });
  });

  describe("t-folder-v5e, 2", function() {
    return it("get file list", function(done) {
      var file_name;
      file_name = '.gg.members.json';
      return fm.retrieve_promisified_folder(folder_path).then(function(folder) {
        return folder.get_file_objs_by_name(file_name, function(e, objs) {
          u.each(objs, function(o) {
            return o.read_to_string();
          });
          return done();
        });
      });
    });
  });

  describe("t-folder-v5e, 3", function() {
    return it("get file meta by uuid", function(done) {
      var dirname, file_path;
      file_path = "abc/goodagood";
      dirname = "abc";
      return done();
    });
  });

}).call(this);
