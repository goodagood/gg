
/*
 * Functions to read the gg defaults in s3.
 */


/*
 * Read a file, this would be regular goodagood user file.  User can 
 * access as normal.  For other  files, we can put it to 'direct files',
 * then user will have no access.
 *
 */
function read_file(fpath, callback){
    // retrieve file and get it's content

}
